<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/cluster_location_size"
    android:layout_height="@dimen/cluster_location_size"
    android:background="@drawable/background_cluster_location"
    android:padding="2dp">

    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/cluster_location_size"
        android:layout_height="@dimen/cluster_location_size"/>

    <TextView
        android:id="@id/amu_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:alpha=".8"
        android:background="@android:color/black"
        android:paddingLeft="2dp"
        android:paddingRight="2dp"
        android:textAppearance="@style/amu_Bubble.TextAppearance.Light"
        android:textColor="@android:color/white" />

</FrameLayout>